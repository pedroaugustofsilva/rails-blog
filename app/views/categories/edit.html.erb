<div class="center-content 2xl:col-start-5 2xl:col-end-9">
  <h1 class="font-bold text-4xl">Editing category</h1>

  <%= render "form", category: @category %>

  <div class="flex justify-end mt-5">
    <%= link_to "Show this category", @category, class: "btn-secondary" %>
    <%= link_to "Back to categories", categories_path, class: "ml-1 btn-secondary" %>
  </div>
</div>
